<h1>Listing proaccounts</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @proaccounts.each do |proaccount| %>
  <tr>
    <td><%= link_to 'Show', proaccount %></td>
    <td><%= link_to 'Edit', edit_proaccount_path(proaccount) %></td>
    <td><%= link_to 'Destroy', proaccount, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Proaccount', new_proaccount_path %>
